<div class="-ml-2 mt-6 -mb-2 border-t-2 border-blue-100 text-sm font-medium">
    @if($subtitle)
        <span class="bg-blue-50 px-2 py-1 inline-block uppercase text-gray-900">{{$subtitle}}</span>
    @endif
</div>
